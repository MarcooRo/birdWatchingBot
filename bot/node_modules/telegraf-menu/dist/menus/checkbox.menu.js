"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.CheckboxMenu = void 0;
var const_1 = require("../const");
var generic_menu_1 = require("../generic-menu");
var CheckboxMenu = (function (_super) {
    __extends(CheckboxMenu, _super);
    function CheckboxMenu(config) {
        var _this = _super.call(this, config) || this;
        _this.config = config;
        return _this;
    }
    CheckboxMenu.prototype.stateToMenu = function (state) {
        var allButtons = this.flatFilters;
        var newButtons = [];
        var currentState = Array.isArray(state)
            ? state
            : [];
        if (currentState.length) {
            var checkboxButton = allButtons
                .filter(function (button) { return currentState.includes(button.value); });
            newButtons.push.apply(newButtons, checkboxButton);
        }
        return newButtons.filter(Boolean);
    };
    CheckboxMenu.prototype.menuToState = function (menu) {
        return menu.map(function (button) { return button; });
    };
    CheckboxMenu.prototype.onActiveButton = function (ctx, activeButton) {
        var activeButtons = this.stateToMenu(this.state).map(function (button) { return button.value; });
        var buttonIndex = null;
        activeButtons.some(function (button, index) {
            var isButtonInList = button === activeButton.value;
            if (isButtonInList) {
                buttonIndex = index;
                return true;
            }
        });
        if (buttonIndex || buttonIndex === 0) {
            activeButtons.splice(buttonIndex, 1);
        }
        else {
            activeButtons.push(activeButton.value);
        }
        _super.prototype.toggleActiveButton.call(this, ctx, activeButtons);
    };
    CheckboxMenu.prototype.formatButtonLabel = function (ctx, button) {
        var CHECKBOX_FORMATTING = const_1.FORMATTING_EMOJIS.CHECKBOX_FORMATTING;
        var _a = _super.prototype.getButtonLabelInfo.call(this, ctx, button), label = _a.label, isActiveButton = _a.isActiveButton;
        return isActiveButton ?
            CHECKBOX_FORMATTING.active + ' ' + label :
            CHECKBOX_FORMATTING.disabled + ' ' + label;
    };
    return CheckboxMenu;
}(generic_menu_1.GenericMenu));
exports.CheckboxMenu = CheckboxMenu;
//# sourceMappingURL=checkbox.menu.js.map